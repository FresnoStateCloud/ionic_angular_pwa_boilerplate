import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'dxi-boilerplate',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
